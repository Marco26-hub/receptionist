import { CalendarDays, ChevronLeft, MessageSquareText, UsersRound } from "lucide-react";
type Row = Record<string, string | boolean>;
export function AdminSection({ title, eyebrow, rows, columns, kind }: { title: string; eyebrow: string; rows: Row[]; columns: Array<{ key: string; label: string }>; kind: "customers" | "appointments" | "messages" }) { const Icon = kind === "customers" ? UsersRound : kind === "appointments" ? CalendarDays : MessageSquareText; return <main className="admin-section-page"><a href="/admin"><ChevronLeft size={17} />Torna alle opportunità</a><header><div><span>{eyebrow}</span><h1>{title}</h1></div><Icon size={28} /></header><div className="admin-data-table"><div className="data-row data-head">{columns.map((column) => <span key={column.key}>{column.label}</span>)}</div>{rows.map((row, index) => <div className="data-row" key={String(row.id || index)}>{columns.map((column) => <span key={column.key}>{typeof row[column.key] === "boolean" ? row[column.key] ? "Sì" : "No" : String(row[column.key] ?? "—")}</span>)}</div>)}</div></main>; }

