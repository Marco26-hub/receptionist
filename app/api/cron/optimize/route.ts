import { runDailyOptimization } from "../../../lib/repository";
export async function GET(request: Request) { const secret = process.env.CRON_SECRET; if (process.env.NODE_ENV === "production" && (!secret || request.headers.get("authorization") !== `Bearer ${secret}`)) return Response.json({ ok: false }, { status: 401 }); const result = await runDailyOptimization(); return Response.json({ ok: true, ...result, completedAt: new Date().toISOString() }); }
